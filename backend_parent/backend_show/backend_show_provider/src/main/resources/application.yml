server:
  port: 7401

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: hello-eureka-provider

logging:
  config: classpath:logback.xml
